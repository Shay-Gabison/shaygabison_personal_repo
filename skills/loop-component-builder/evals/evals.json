{
  "skill": "loop-component-builder",
  "cases": [
    {
      "prompt": "Create a Loop component with this 25-row repository ownership table.",
      "expected": [
        "Navigates directly to the signed-in Loop web app",
        "Uses at most two browser calls in the normal flow",
        "Creates a real Loop page with all 25 rows",
        "Uses tab-separated plain text instead of the dynamic insert menu",
        "Does not create a TypeScript or Teams app project",
        "Returns the private Loop page URL and elapsed time"
      ]
    },
    {
      "prompt": "Put these action items into Loop.",
      "expected": [
        "Infers a checklist or task-list format",
        "Asks no question when the action items are supplied",
        "Uses visible checkbox markers in fast mode",
        "Does not open the slash insert menu",
        "Creates and verifies the visible Loop artifact",
        "Targets less than 60 seconds",
        "Returns a concise link"
      ]
    },
    {
      "prompt": "Create this Loop quickly.",
      "expected": [
        "Skips API and documentation research",
        "Uses the existing signed-in Loop browser session",
        "Uses the consolidated fast script",
        "Reports measured elapsed time"
      ]
    },
    {
      "prompt": "Build me a custom Adaptive Card-based Loop component SDK project.",
      "expected": [
        "Explains that developer mode creates source code, not an immediate Loop page",
        "Requests confirmation before entering developer mode",
        "Uses current official Microsoft guidance only after confirmation"
      ]
    },
    {
      "prompt": "Create a Loop.",
      "expected": [
        "Asks once for title and content",
        "Does not ask about hosting, SDK, persistence, SSO, or deployment"
      ]
    }
  ]
}
