# Configuration: spec-driven-development

The install script copied `spec-driven-config.md` to your project root.

Open [templates/spec-driven-config.md](../templates/spec-driven-config.md) — each placeholder has inline comments explaining what it is and how to find the value.

Fill in all `{{PLACEHOLDER}}` values in your copied `spec-driven-config.md`.
